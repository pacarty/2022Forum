@using WhirlForum2.Models

@model TopicModel

<h1>@Model.Name</h1>

<table>
    <tr>
        <th></th>
        <th>Creator</th>
    </tr>
@foreach (var post in Model.PostModels)
{
    <tr>
        <td><a asp-controller="Forum" asp-action="Post" asp-route-id="@post.Id">@post.Name</a></td>
        <td>@post.UserModel.Username</td>
    </tr>        
}
</table>
<br />

@Html.Partial("_TopicPagination", Model.PaginationInfo)

<p><a asp-controller="Forum" asp-action="NewPost" asp-route-id="@Model.Id">New Post</a></p>